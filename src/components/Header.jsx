
import headerStyle from '../scss/header.module.scss';

const Header = () => {
  return (
    <header>
      <h1 className={headerStyle['title']}>Project - Team Members </h1>
    </header>
  );
};

export default Header;
